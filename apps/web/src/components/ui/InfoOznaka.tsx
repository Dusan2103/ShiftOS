import { Info } from 'lucide-react';

export function InfoOznaka() {
  return (
    <span className="info-oznaka neu-icon-circle w-6 h-6 text-[var(--color-text-muted)] pointer-events-none">
      <Info size={12} />
    </span>
  );
}
